# Nginx reverse proxy for ScholarZim
# Place at: /etc/nginx/sites-available/scholarzim.co.zw
# Then: sudo ln -s /etc/nginx/sites-available/scholarzim.co.zw /etc/nginx/sites-enabled/
# Obtain TLS first (or use certbot --nginx after this file exists):
#   sudo certbot --nginx -d www.scholarzim.co.zw -d scholarzim.co.zw

server {
    listen 80;
    listen [::]:80;
    server_name scholarzim.co.zw www.scholarzim.co.zw;

    # Certbot will replace this with HTTPS redirects when using --nginx
    location / {
        return 301 https://www.scholarzim.co.zw$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name scholarzim.co.zw;

    # TLS paths filled by Certbot, or set manually:
    # ssl_certificate     /etc/letsencrypt/live/www.scholarzim.co.zw/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/www.scholarzim.co.zw/privkey.pem;

    return 301 https://www.scholarzim.co.zw$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.scholarzim.co.zw;

    # ssl_certificate     /etc/letsencrypt/live/www.scholarzim.co.zw/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/www.scholarzim.co.zw/privkey.pem;

    client_max_body_size 12m;   # matches the app container nginx and post_max_size

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_read_timeout 120s;
    }
}

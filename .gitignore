# Laravel
/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.env.prod
# Filled-in compose env: holds a real APP_KEY and Mailgun secret, so it is a
# working credential file rather than a template. The template is .env.example.
.env.docker
# Local QA credentials file: holds real account passwords for production QA and
# must never be committed. Kept out of the tree entirely.
qa-credentials.env
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log

# IDE
/.fleet
/.idea
/.vscode
*.iml

# OS
.DS_Store
Thumbs.db

# Local dev artifacts (never commit)
cookies.txt
debug-*.log

# Database dumps/backups (may contain real local data)
**/database/backups/
